import { setRequestLocale } from "next-intl/server"; export default async function Security({params}:{params:Promise<{locale:string}>}){const {locale}=await params;setRequestLocale(locale);return <section><div className="mx-auto max-w-3xl px-6 py-24 md:py-32"><p className="kicker">Security</p><h1 className="font-display mt-5 text-5xl">Security principles</h1><p className="mt-7 leading-8 text-paper-soft">The public website uses HTTPS, security headers, controlled content publication and a separate platform boundary. Internal Core architecture, credentials, prompts and protected methodology are not exposed.</p></div></section>}