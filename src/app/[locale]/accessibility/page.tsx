import { setRequestLocale } from "next-intl/server"; export default async function Accessibility({params}:{params:Promise<{locale:string}>}){const {locale}=await params;setRequestLocale(locale);return <section><div className="mx-auto max-w-3xl px-6 py-24 md:py-32"><p className="kicker">Accessibility</p><h1 className="font-display mt-5 text-5xl">Accessible by design</h1><p className="mt-7 leading-8 text-paper-soft">The website targets WCAG 2.2 AA practices including keyboard navigation, visible focus, semantic structure, readable contrast and support for enlarged text. Arabic uses a real RTL document direction.</p></div></section>